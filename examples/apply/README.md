# Apply (examples index)

The runnable demo lives at [`../../samovar/samovar_apply`](../../samovar/samovar_apply/) (`pipeline.sh` plus README). It is kept there to match the Apply BIM layout; this folder exists so it also appears in the examples table.
