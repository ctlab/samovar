% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_annotation.R
\name{remap_annotation_rank}
\alias{remap_annotation_rank}
\title{Collapse taxIDs to an NCBI rank via a cached \code{taxid|genera_taxid} table.}
\usage{
remap_annotation_rank(data, rank = "genus", python = NULL, cache_file = NULL)
}
\arguments{
\item{data}{annotation table with \verb{taxID_*} / \verb{taxid_*} and optional \code{true}.}

\item{rank}{NCBI rank (default \code{genus}). \code{none} / \code{exact} keeps original IDs.}

\item{python}{optional python executable. Falls back to \code{PYTHON_PATH} then \code{python3}.}

\item{cache_file}{path to a pipe-separated \code{taxid|genera_taxid} table.}
}
\value{
the same table with taxIDs remapped in memory, or the original table if remapping fails.
}
\description{
Original annotation tables are not modified. Lookups go through Python ete3
and are stored in \code{cache_file} (and the user cache) for reuse.
}
