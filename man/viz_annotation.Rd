% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_annotation.R
\name{viz_annotation}
\alias{viz_annotation}
\title{Visualize annotation results}
\usage{
viz_annotation(
  data,
  type = c("f1", "R2", "cv", "conf"),
  show_top = 10,
  output_dir = NULL,
  plot = T,
  split = T,
  rank = "genus",
  use_names = TRUE,
  reord = "fpc"
)
}
\arguments{
\item{data}{Processed abundance table.
Row names: sequence IDs,
Column names:
\itemize{
\item annotators: (starting with \code{taxID_});
\item \code{true}: for true annotation
\item \code{length}: length of sequence
\item sample
}}

\item{type}{character vector.
\itemize{
\item if present column true_annotation: could be one of
\itemize{
\item "f1",
\item "R2",
\item "confidence",
\item "cross-validation",
\item or their combination (e.g. c("f1", "R2", "cv", "conf"))
}
}}

\item{show_top}{integer. Number of top annotations to show.}

\item{output_dir}{character. Directory to save the plots. If NULL, plots are not saved.}

\item{plot}{logical. If TRUE, plots are printed.}

\item{split}{logical. If TRUE, plots are split into separate files.}

\item{rank}{NCBI rank used to regroup taxIDs before F1 / R2 / CV (default genus).
Use \code{none} to compare exact taxIDs. Lookup uses ete3 via Python and is
cached as \code{taxid|genera_taxid} next to the plots; source tables are unchanged.}

\item{use_names}{logical. If TRUE (default), axis labels are scientific names.
Genus and species names are drawn in italic; \code{0} and \code{other} are not.}

\item{reord}{character. Shared row/column order for F1 and CV heatmaps.
\code{fpc} (default) uses the first principal component of the count matrix;
\code{0} and \code{other} are excluded from that sort and appended last.}
}
\value{
list of ggplot objects
}
\description{
Visualize annotation results
}
\examples{
library(samovaR)

data <- read_annotation_dir(system.file("extdata", "test_annotations", package = "samovaR"))
gglist <- viz_annotation(data)

samovar_list <- annotation2samovar(data)
}
